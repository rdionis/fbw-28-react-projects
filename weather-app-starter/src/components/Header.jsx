import React from "react";

const Header = () => (
  <div className="header">
    <h1 className="header__title">Weather Finder</h1>
  </div>
);

//here we don't need the curly braces or the 'return' keyword since we're only returning some jsx markup ---> we just need () and we can return jsx inside

export default Header;
